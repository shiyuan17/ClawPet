# DragonClaw Bootstrap

This workspace is managed by DragonClaw.

- Runtime: bundled OpenClaw
- Token: persisted in `~/.openclaw/openclaw.json`
- Gateway: controlled by DragonClaw install/upgrade/restart actions
